{
  "name": "tracker",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.23.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "pnpm turbo run dev",
    "build": "pnpm turbo run build",
    "lint": "pnpm turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.6.3"
  }
}
